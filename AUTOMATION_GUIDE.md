# PDF 自动化处理系统使用指南

## 🎯 系统架构

### 完整工作流程

```
本地 pdfs_to_process/
    ↓ (手动上传)
GitHub pdfs_to_process/
    ↓ (每天早上 6:00 自动处理一篇)
GitHub public/pdfs/ + 文章数据
    ↓ (自动部署)
网站 www.liliailab.cn
```

---

## 📋 使用步骤

### 步骤 1：准备 PDF 文件

将所有待处理的 PDF 文件放入本地 `pdfs_to_process/` 目录：

```bash
# 示例：复制 PDF 到目录
cp ~/Downloads/*.pdf pdfs_to_process/
```

---

### 步骤 2：批量上传到 GitHub

在项目根目录执行上传脚本：

```bash
cd "/Users/lesley/Desktop/GitHub/Research Web/research-website"

./upload-pdfs.sh
```

**脚本会自动：**
1. ✅ 统计并列出所有 PDF 文件
2. ✅ 询问确认
3. ✅ 提交并推送到 GitHub
4. ✅ 将本地 PDF 移动到 `pdfs_archived/` 归档

---

### 步骤 3：自动处理（无需操作）

**GitHub Actions 会自动：**
- ⏰ 每天早上 6:00（北京时间）自动运行
- 📄 处理 `pdfs_to_process/` 中的第一个 PDF
- 🤖 调用 DeepSeek API 提取内容
- 📝 生成文章数据和详情页
- 📦 移动 PDF 到 `public/pdfs/`
- 🚀 自动提交并部署到网站

---

## 🛠️ 命令速查

### 上传 PDF 到 GitHub
```bash
./upload-pdfs.sh
```

### 查看待处理的 PDF（GitHub）
```bash
# 访问
https://github.com/Lili202602/research-website/tree/main/pdfs_to_process
```

### 手动触发处理（不等到早上 6:00）
```bash
# 访问
https://github.com/Lili202602/research-website/actions/workflows/daily-insight.yml
# 点击 "Run workflow"
```

### 查看本地归档
```bash
ls -lh pdfs_archived/
```

---

## 📊 文件流转说明

### 本地文件夹

| 文件夹 | 用途 | 说明 |
|--------|------|------|
| `pdfs_to_process/` | 待上传 | 放入新 PDF，运行 `./upload-pdfs.sh` 上传 |
| `pdfs_archived/` | 本地归档 | 上传后自动移动到这里 |

### GitHub 文件夹

| 文件夹 | 用途 | 说明 |
|--------|------|------|
| `pdfs_to_process/` | 待处理队列 | 每天早上 6:00 处理一篇 |
| `public/pdfs/` | 已发布 | 处理后的 PDF 存放位置 |
| `posts/` | 文章详情 | 生成的 HTML 文章 |
| `src/data/articlesData.ts` | 文章数据 | 网站首页显示的数据 |

---

## 🔄 典型使用场景

### 场景 1：批量上传 5 篇 PDF

```bash
# 1. 复制 PDF 到目录
cp ~/Downloads/报告*.pdf pdfs_to_process/

# 2. 上传
./upload-pdfs.sh

# 3. 等待自动处理（每天一篇，5 天处理完）
```

### 场景 2：紧急发布一篇文章

```bash
# 1. 上传 PDF
./upload-pdfs.sh

# 2. 手动触发 GitHub Actions
# 访问: https://github.com/Lili202602/research-website/actions
# 点击 "Daily Insight Auto Publish" → "Run workflow"

# 3. 等待 5-10 分钟，网站自动更新
```

### 场景 3：查看处理进度

```bash
# 查看 GitHub 上还有多少待处理
# 访问: https://github.com/Lili202602/research-website/tree/main/pdfs_to_process

# 查看最近的处理记录
# 访问: https://github.com/Lili202602/research-website/actions
```

---

## ⚙️ 系统配置

### GitHub Actions 定时任务

文件：`.github/workflows/daily-insight.yml`

```yaml
schedule:
  - cron: '0 22 * * *'  # UTC 22:00 = 北京时间 6:00
```

### 处理模式

- **单篇模式**：每次只处理一个 PDF（`--single` 参数）
- **批量模式**：处理所有 PDF（不推荐，可能超时）

---

## 🐛 故障排查

### 问题 1：上传脚本报错 "permission denied"

**解决**：
```bash
chmod +x upload-pdfs.sh
```

### 问题 2：GitHub Actions 没有自动运行

**检查**：
1. 确认 `pdfs_to_process/` 中有 PDF 文件
2. 查看 Actions 日志：https://github.com/Lili202602/research-website/actions
3. 确认 `DEEPSEEK_API_KEY` Secret 已配置

### 问题 3：处理后网站没有更新

**检查**：
1. 查看 Actions 是否成功（绿色 ✓）
2. 确认 `src/data/articlesData.ts` 已更新
3. 清除浏览器缓存，刷新网站

---

## 📈 最佳实践

### 1. 文件命名规范

建议使用清晰的文件名：
```
【机构名】报告标题.pdf
```

示例：
```
【麦肯锡】2026年供应链数字化转型报告.pdf
【德勤】全球物流趋势洞察.pdf
```

### 2. 批量上传建议

- ✅ 一次上传 5-10 篇（推荐）
- ⚠️ 避免一次上传超过 20 篇
- ⚠️ 单个文件不超过 50MB

### 3. 定期检查

- 每周检查一次 Actions 运行状态
- 每月清理 `pdfs_archived/` 归档文件

---

## 🎉 快速开始

现在就试试吧！

```bash
# 1. 进入项目目录
cd "/Users/lesley/Desktop/GitHub/Research Web/research-website"

# 2. 放入 PDF 文件
cp ~/Downloads/报告.pdf pdfs_to_process/

# 3. 上传
./upload-pdfs.sh

# 4. 等待自动处理或手动触发
```

---

## 📞 需要帮助？

- 查看 Actions 日志：https://github.com/Lili202602/research-website/actions
- 查看故障排查指南：`TROUBLESHOOTING.md`
- 查看恢复指南：`RECOVERY_GUIDE.md`

